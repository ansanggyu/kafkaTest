spring:
  application:
    name:kafkaTest
  datasource:
    url: jdbc:mariadb://localhost:3306/test
    username: root
    password: test
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

kafka:
  bootstrap-servers: localhost:9092

logging:
  level:
    com.example.kafkatest.controller.TableController: error